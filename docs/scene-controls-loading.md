# シーン切り替え中の操作ボタン（2026-09-10）

最初の画像表示後は、シーン切り替えや読み込み失敗で操作ボタンが消えないようにした。テーマ変更は読み込み中も使える。全画面・ホーム・画質・対応端末のジャイロは、位置を保ち、画像の準備ができるまで無効になる。

## 変更

- ボタンの表示条件と操作可能条件を分離。初回画像の準備前は画像操作を表示せず、一度表示した後は読み込み・失敗中も配置を維持する。
- 画像のない状態では表示履歴をリセット。編集時の全画面・ジャイロ非表示、2Dでのジャイロ非表示など、適用されない操作の条件は維持する。
- 読み込み・暗転表示を画像とUIの間に配置し、テーマボタンが覆われる問題を解消。
- ジャイロ対応状況をビューア再生成中も保持し、読み込み中に別の状態更新で誤って有効化されないようにする。
- 画像配信・キャッシュ・先読み・GAS APIの処理は変更していない。

## 検証

- 新しいブラウザーテストで、変更前にテーマボタンが読み込み表示に覆われることを再現。
- Nodeテスト424件成功。
- 関連ブラウザーテスト49件のうち48件が初回成功。既存のドラッグ並べ替えテスト1件はreorderScenes呼び出しが発生せず失敗したが、コードを変えずに単独再実行すると成功。今回追加した8件はすべて成功。
- テーマ、全画面、画面幅、編集モード、コンテンツ位置のレイアウトテスト153件成功。
- 追加テストではPC幅1200px・スマホ幅390px、360→2D・2D→360、遅いGAS応答、画像読み込み失敗と再試行、編集モード、ジャイロ、画像のない状態を確認。
- 接続済みテストGASの更新前7ファイルを読み戻し、HEADと一致することを確認してバックアップ。clasp push --forceで開発版/devへ反映後、再度読み戻してローカル7ファイルと一致することを確認。
- ログイン済みChromeで/dev?mode=public&delivery=directを開き、「0416_1年棟、駐輪場付近」から「0416_2・3年駐輪場奥」へ移動。読み込み中も全画面・ホーム・画質ボタンが表示されたままdisabledになり、テーマ変更が成功。読み込み完了後に操作可能へ戻り、ブラウザーのエラーログは0件。

公開/execのバージョン更新は行っていない。実機での確認はChromeのPC表示であり、実際のスマートフォンのジャイロ動作までは検証していない。
