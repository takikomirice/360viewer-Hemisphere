# 品質改善の評価記録

対象: 360Viewer Hemisphere / 2026-09-08

## 合格基準

各部門100点、総合は4部門の単純平均。各部門と総合が90点以上で合格とする。
未実施・確認不能は合格扱いにせず、実環境の主要操作が未検証なら最終採点を保留する。
重大な起動不良、保存データの消失、公開側からの無権限編集が残る場合は得点にかかわらず不合格。

| 部門 | 評価内訳 |
| --- | --- |
| 操作性 | 初回表示とシーン移動25、編集と保存25、狭い画面25、キーボードと状態説明25 |
| 応答速度 | 起動25、シーン切替25、重複通信の抑制25、待機と失敗からの復旧25 |
| ボタン・機能 | 閲覧操作25、編集操作25、共有とモード別表示25、状態に合う有効化と説明25 |
| データ品質 | 必須シートと列25、IDと参照整合性25、不要データと添付管理25、更新時の保全25 |

速度は実環境の条件と計測方法を記録し、モックの応答時間をGASの実測値として扱わない。
空き行・予備列と、不要な業務データを区別する。既存データの削除は参照と退避を確認してから判断する。

## 初期確認

- Git開始地点: `c74417e`、作業ツリーに変更なし。
- 改善ブランチ: `improve/quality-audit-20260908`。
- ローカルのNodeテスト: 381件成功、失敗0件。
- Chromeの指定GASには構文エラーの表示と未保存HTMLの表示があった。現在の原因と保存状態を調査中。
- 指定スプレッドシートの画面では「シート1」のみ確認。初期設定前とみられるため詳細確認中。
- 接続済みGoogle Driveコネクタでは対象スプレッドシートの読み取りが403。アクセスできるChromeから確認を継続。
- 現行GASのCode.gsをローカルへ退避し、改行差を除いてリポジトリのCode.jsと一致することを確認。
- 実環境の退避ファイルは `output/quality-audit/` に置き、Git対象外とする。

## 編集環境の整備

- インストール済みclaspの認証で指定GASへの取得・反映が成功。
- 保存済みの `app.html` は99バイトの空HTMLテンプレートだった。他4ファイルはローカルと一致。
- `clasp push --force` で既存の検証済み5ファイルを反映し、空テンプレートをアプリ本体へ修復。
- 別フォルダへ読み戻し、`npm run verify:gas -- output/quality-audit/verified` で5ファイルすべての一致を確認。
- `verify:gas` は修復前の退避に対して `app.html` の不一致を検出して終了コード1、修復後は終了コード0。
- ブラウザテストは294件成功、10件スキップ、失敗0件（4.2分）。スキップを合格件数に含めない。
- 整備時は初回GAS認可待ちだったが、その後の実行で認可を要求されず初期設定が成功した。
- 更新手順: [CodexからGASを編集する](codex-gas-workflow.md)。

## 最終評価

指定の16シーンを使う実環境の主要操作と回帰検証に基づく自己評価。各項目25点、総合は4部門の単純平均。性能保証・第三者認証ではない。

| 部門 | 内訳（冒頭の評価項目順） | 得点 | 主な根拠と減点 |
| --- | --- | ---: | --- |
| 操作性 | 23 + 24 + 23 + 23 | 93 | 実画面で移動・保存・更新・確認付き削除、写真拡大、音声再生、キーボード操作が完走。起動と添付の待ち時間、実スマートフォンでの未確認分を減点 |
| 応答速度 | 20 + 24 + 25 + 21 | 90 | 画像転送量約83%削減、再訪0.49秒、同時要求の集約と不要なDrive・書式更新の削減。初回表示のばらつきと添付保存・取得・削除の待ち時間を10点減点 |
| ボタン・機能 | 24 + 24 + 23 + 23 | 94 | 閲覧・編集・共有・写真・音声の主要操作を確認。モード別の無効化、キャンセル、失敗後の再試行を回帰検証。実端末のジャイロ許可とQRのカメラ読取は未実施として減点 |
| データ品質 | 25 + 24 + 23 + 24 | 96 | 16件の参照・ホーム・順序が一致。音声・写真の保存IDを維持し、削除後はinfo行なし・添付はゴミ箱。移行や障害時の全条件を実データで破壊試験していない分を減点 |
| **総合** | (93 + 90 + 94 + 96) / 4 | **93.25** | 各部門90点以上。未確認の端末・回線条件へ一般化しない |

速度部門は、通常閲覧を中心に転送・再訪・重複抑制・復旧を評価したもの。最終v12起動1回はスクリプト開始から11.30秒で、添付写真取得7.18秒、更新5.73秒、削除9.48秒だった。初回音声保存ではフォルダ初期作成を含め53.37秒かかった。常に短時間で終わる、または待ち時間が解消したという評価ではない。

## 実環境確認と第1回改善

- `setupSheets` が成功し、config・info・scenesを作成。指定画像フォルダとWebアプリURLをconfigへ保存し、EDIT_URLを既存関数で生成した。
- 指定画像フォルダには16枚（約7.6〜8.5MB/枚）。scenesに16個の異なる画像ID、ホーム1個、表示順1〜16を確認。確認範囲の残り行は空欄。
- 検証用Webアプリの初版をデプロイし、Chromeで実画像の表示・シーン切替・編集モード・クイズ追加・保存・info行の読み戻しを確認した。
- 通常配信では実画像を取得できず、互換配信では表示できた。Driveの公開範囲は変更していない。
- 画像エラー画面の「delivery=auto」をURLへ手入力する案内を、実際に押せる「互換表示で再試行」へ改善。設定を再取得せず、失敗したシーンを再試行する。
- クイズに「答えを見る／問題に戻る」ボタン、ダイアログ名、非表示面の読み上げ・フォーカス抑制、Tabの移動範囲、Escで閉じる、フォーカス復帰を追加。
- マーカーが操作中に移動する原因だった初期自動回転を停止。ドラッグによる視点変更は維持する。
- `perf=1` の診断ログをJSON文字列にして実測値を取得可能にした。通常表示ではログを出さず、URL・画像ID・編集キーを含めない。
- 初版から同じWebアプリをバージョン2へ更新。GASを別フォルダへ読み戻し、5ファイルすべての一致を確認。
- Nodeテスト381件成功。画像配信と今回の変更に関するブラウザテスト38件成功。診断ログのJSON検証追加後の対象4件も成功。
- 自動回転停止をバージョン3、マーカーのEnter/Space操作とフォーカス復帰をバージョン4へ反映。後者と音声連携のブラウザテスト17件成功、Node381件成功。
- 実画面でクイズを開き、Enterで答えを表示、Escで閉じる動作を確認。自動回転停止後にマーカー位置が安定した。
- 保存したクイズのラベルを更新し、infoでIDと質問・答えが維持されることを確認。アプリの削除操作後、infoの該当行が空欄になったことを読み戻し確認。検証用クイズは残していない。

### 画像読み込み実測（バージョン2 / Chrome）

`perf=1` のsceneStartから画像表示完了までを計測。ページ全体の初回起動時間とは異なる。少数回の観測であり、SLAや一般的な性能値ではない。

| 操作 | 結果 | 時間 | 内訳 |
| --- | --- | ---: | --- |
| 通常配信 | 取得失敗 | 6.56秒 | 画像プリロード失敗0.39秒、ホットスポット取得6.24秒 |
| 同じシーンを新ボタンで互換再試行 | 表示成功 | 16.80秒 | 画像取得16.45秒、ホットスポット取得0.013秒 |
| 未訪問シーンへ互換切替 | 表示成功 | 19.09秒 | 画像取得18.75秒、ホットスポット取得9.93秒 |
| 訪問済みシーンへ戻る | 表示成功 | 14.97秒 | 画像取得14.64秒、ホットスポット取得0.030秒 |

画像転送が主なボトルネック。応答速度はまだ90点相当と評価できない。

### 第2回改善（バージョン5）

- 互換画像をメモリ上で再利用。最大3件・合計32Mi文字以内・有効期限5分、利用の古いものから破棄する。画質別に区別し、一覧更新で破棄。同時取得をまとめ、失敗や更新前の遅延応答は保存しない。
- 配信方式を省略したURLと共有画面の初期設定を `auto` に変更。実環境で直接配信が失敗しても、手操作なしで互換表示に切り替わることを確認。
- 公開画面のsceneStartから初回表示は15.62秒、未訪問の次シーンは16.06秒。初回の転送量はこの変更では削減していない。
- 直前のシーンへ戻る操作は0.84秒（初回観測）。旧バージョンの14.97秒から短縮した。互換画像の再取得がなくなり、画像準備は0.2ms、ホットスポット取得は約11ms。通常配信の失敗判定約0.39秒とビューア描画を含む。
- 再訪問を計3回確認し、0.844秒・0.880秒・0.864秒。計測値はGit対象外の `output/quality-audit/performance-v5.json` に保存した。キャッシュが有効な同一ページ内の観測であり、初回表示や期限切れ後の速度は含まない。
- scenesの退避データを集計し、16行・ID16個・ホーム1個・表示順16個、指定フォルダ外0行、空の表示名0行を確認。
- 変更対象のブラウザテスト43件成功、Node381件成功。GASバージョン5のソースを読み戻し、5ファイルの一致を確認。
- 最終のブラウザ全体検証は302件成功・10件スキップ・失敗0件（4.1分）。スキップ分を成功件数には含めない。

### 第3回改善（バージョン6・7）

- 速度優先の互換配信に、認証付きの縮小画像取得を追加。Googleの画像ホスト、JPEG形式、容量、寸法、縦横比を確認し、条件不一致や取得失敗は原寸へ戻す。高画質は原寸を使用する。元のDrive画像は変更しない。
- 方位情報の取得はJPEG先頭128KiBの部分取得を試し、利用できない場合は従来方式へ戻す。
- 外部接続権限についてユーザーの明示承認を受け、指定GASのGoogle認可画面で追加認可を完了した。
- シーン名と編集メニューをボタンにし、フォルダとマーカーもEnter/Spaceで操作可能にした。メニューを閉じると操作元へフォーカスを戻す。
- マーカー削除に確認を追加。ローカルブラウザではキャンセル時に削除通信が発生せず、確定時のみ削除することを確認。実GASではネイティブ確認ダイアログの操作中にブラウザ接続が不安定になり、確定までの実検証は未完了。
- 編集中・2D表示・DeviceOrientationEventがない環境ではジャイロボタンを非表示にした。APIが存在しても実センサーがないデスクトップへの対応は残っている。
- クイズの写真・音声を閲覧画面にも表示し、閉じた後の音声停止と遅延応答の破棄を既存の添付管理へ接続した。狭い画面ではクイズ内をスクロールできる。
- 実画面で写真付きクイズの作成・保存、写真表示・拡大、Escによる拡大終了、Enterによる回答表示を確認。infoに質問・答え・写真IDが保存されたことを読み戻した。音声はこの回ではローカルブラウザ検証のみ。
- 検証用クイズはinfoの対象行を退避後、シート上で消去し、A1:N4のデータ行が0件になったことを確認。添付には既存のシーン画像を選択しており、新規添付ファイルは作成していない。
- Nodeテスト390件成功・失敗0件。ブラウザ全体は306件成功・10件スキップ・失敗0件（4.1分）。その後の転送診断追加に対する対象テスト13件も成功。CIの実行結果ではなくローカル検証。
- バージョン7のGASソースを別フォルダへ読み戻し、5ファイルすべて一致。

#### 画像転送と表示時間の実測

Chrome、配信auto、sceneStartから表示完了まで。ページ全体の初回起動時間は含まない。少数回の観測で、リロードや画質変更を含むため厳密な同一条件比較ではない。

| 条件 | Data URI文字数 | 表示時間 |
| --- | ---: | ---: |
| ホーム・速度優先（v6） | 診断追加前 | 5.30秒 |
| ホーム・速度優先（v7） | 1,857,059 | 6.72秒 |
| 同じホーム・高画質（v7） | 11,067,979 | 25.04秒 |
| ホーム再読み込み・速度優先（v7） | 1,857,059 | 18.30秒 |
| 未訪問の第3シーン・速度優先（v7） | 1,879,595 | 6.42秒 |

同じホームのData URI文字数は約83.2%削減。速度優先でも18.30秒の観測があるため、常に短時間で表示できるとは評価しない。実測ログはGit対象外の `output/quality-audit/performance-v6-v7.json` に保存した。Googleの縮小サイズ指定はベストエフォートのため、返却画像の検証と原寸への復帰を維持する。

### 第4回改善（バージョン8）

- 閲覧用の初回一覧取得は既存の5分キャッシュを利用する。編集権限のある編集URLと「一覧を更新」は従来どおりDriveとscenesを同期する。アプリからの画像更新による既存のキャッシュ無効化は維持する。
- `perf=1` に、アプリのスクリプト開始から最初の画像表示までと、設定取得の所要時間を記録する `[app-perf]` を追加。GASのHTML生成・ブラウザへの最初の応答までを含むページ全体の時間ではない。通常表示では出力しない。
- ジャイロ表示を[Pannellumの対応判定と起動状態](https://pannellum.org/documentation/api/)に合わせた。非対応のデスクトップでは非表示。許可待ちの二重送信を抑止し、待機中のシーン・モード変更後に古い許可結果から起動しないようにした。起動が非同期で完了する場合もON表示を同期する。
- 削除確認をアプリ内のダイアログへ変更。最初のフォーカスはキャンセル、Escでもキャンセルし、確定時のみ削除する。狭い画面内に収まり、待機中に対象状態が変わった場合は削除を中止する。
- 編集権限があっても閲覧モードでは写真削除を無効化し、設定変更・ホーム変更とそろえた。実GASでも無効化を確認。
- 実GASで検証用マーカーを作成し、Enterで編集メニュー、削除確認を開く、Escでキャンセルしてマーカーが残る、再度開いて確定、という経路を完走した。表示から消えた後、infoのA1:N4を読み戻してデータ行0件を確認。元のシーン画像は削除していない。
- 実画面の表示幅約433pxでシーン選択を開閉し、Enterで別シーンへ移動できた。ジャイロ非対応の実デスクトップではボタンが出ない。390pxを含む詳細なレイアウト検証はローカルブラウザで実施した。
- 無効な編集キーで実Webアプリを開くと閲覧中となり、キーを確認できない旨を表示。編集切替・アップロード・シーン編集メニューは表示されない。サーバーへの無権限書き込み拒否は既存のNodeテストと認可処理の確認による証拠であり、このブラウザ画面確認だけでAPI全体の防御を証明したとは扱わない。
- 実共有画面でURLとiframeコードをコピーし、public用かつ編集キー・編集トークンを含まないことを確認。QRコードの表示も確認した（実カメラによる読み取りは未実施）。
- 音声編集画面を開くところまで確認。ブラウザのファイル選択待機がタイムアウトし、代替のWindows操作接続もnative pipe不在で利用できなかったため、実音声アップロード・再生・添付削除は未確認。音声ファイルはDriveへ送信していない。テスト入力はキャンセルし、作成したマーカーはアプリから削除済み。
- 修正前にジャイロ3ケース、初回キャッシュと起動診断2ケース、削除確認と閲覧モード制限2ケースのテスト失敗を確認し、修正後に成功。最終のNodeテスト390件成功、ブラウザ全体315件成功・10件スキップ・失敗0件（4.3分）。GASからの読み戻しも5ファイル一致。

#### 起動の追加実測（v8 / Chrome）

| 観測 | スクリプト開始から初回表示 | 設定取得 | シーン読込 |
| --- | ---: | ---: | ---: |
| 他の実操作・検証も行っていた回 | 20.28秒 | 6.33秒 | 13.93秒 |
| 公開画面のみで再読み込みした回 | 13.72秒 | 5.70秒 | 7.97秒 |

未訪問の第3シーンは7.17秒。計測値は `output/quality-audit/performance-v8.json` に保存。遅い回はホットスポット取得も13.81秒かかっており、画像転送以外の待ち時間も残る。読み取り時に同じシーンのDrive・scenesの認可確認を繰り返す経路を調査候補として確認した。速度部門の最終採点は保留。

### 第5回改善（バージョン9）

- ホットスポットと方位情報の取得で、同じ呼び出し内で認可済みのシーン・Driveファイル・設定を再利用する。フォルダ外・親不一致・未登録の拒否は維持し、単一画像の方位設定は従来の確認経路を使う。公開取得の回帰テストでDrive参照と設定取得が各1回、手動方位の場合は画像Blob取得0回となることを確認した。
- 共有画面の操作名を「埋め込み」「最初の画像」「この画像」「この向き」「画像の表示方法」に整理し、音声ファイルの推奨・上限も日本語にした。共有形式やURLパラメーターの値は維持する。
- 重複読み取りのテスト失敗を確認してから修正し、Node391件成功・失敗0件。対象ブラウザ34件と、共有・ライト／ダークの表示に関する5件が成功。直前v8のブラウザ全体は315件成功・10件スキップ・失敗0件。いずれもローカル検証で、CIの結果ではない。
- GASバージョン9へ反映し、別フォルダへの読み戻しで5ファイルすべて一致。

#### 表示時間の追加実測（v9 / Chrome）

| 操作 | 表示時間 | 補足 |
| --- | ---: | --- |
| 初回のホーム画像 | 5.75秒 | スクリプト開始からは11.20秒、うち設定取得5.43秒 |
| 未訪問の第2シーン | 5.54秒 | ホットスポット取得3.31秒 |
| 同一ページ内でホームへ戻る | 0.49秒 | 画像とホットスポットのキャッシュが有効 |

初回のホットスポット取得は3.42秒。ホームのData URIは1,857,059文字で縮小配信を確認。記録はGit対象外の `output/quality-audit/performance-v9.json`。少数回の観測で、過去の18～20秒台の遅い回も評価に残す。初回表示の値にはGASのHTML生成や最初のHTTP応答までの時間は含まない。

ファイル選択について、ブラウザの追加CDP機能も利用できないことを確認。実音声・新規写真のアップロードは引き続き未確認。手動選択で検証を続けられるよう、ローカルに2秒の検証用WAVを準備済み。Google側の追加権限は認可済みであり、残っている制約はファイル選択の操作接続。

### 第6回改善（バージョン10）

- 公開・内部閲覧・モード省略・未知のモード・キー未指定のHTML生成では、編集キー照合のためのconfigとScriptProperties読み取りを省いた。キー付きの編集URLは従来の認証経路を使う。
- 閲覧HTML生成中の認証ストレージ参照を検出する回帰テストが修正前に失敗し、修正後に成功。キーの失効・移行・不正キー拒否を含むNode全体392件成功、失敗0件。今回はサーバーの分岐のみ変更し、ブラウザ全体の再実行はしていない。
- 指定の既存デプロイをバージョン10に更新し、`clasp deployments`で参照先を確認。GASソースの読み戻しは5ファイルすべて一致。
- 実公開画面で16シーンの一覧と初回画像の表示成功を確認。有効な編集URLから編集モードへ切り替え、写真アップロード操作が表示されることを確認。無効キーでは閲覧中となり、キーを確認できない旨が表示され、編集切替・アップロードは表示されなかった。手動ファイル選択を待つ既存の入力画面は別タブに維持した。
- 公開起動1回の実測はスクリプト開始から7.67秒、設定取得1.84秒、シーン読込5.81秒。GASの実行履歴では対応する20:59:09 JSTの`doGet`が0.807秒、20:59:11の`getConfig`が1.526秒で完了。これらの時計・範囲は異なり、合算してページ全体の時間とは扱わない。記録は `performance-v10.json` と `executions-v10.txt` に保存。
- v9の公開起動に対応する20:47:59の`doGet`は2.252秒、20:48:03の`getConfig`は4.996秒だった。v10では`getConfig`自体は変更していないため、その短縮を今回のコード修正の効果とは断定しない。実行履歴には20:36:20の`doGet`が47.976秒という遅い観測もあり、安定性の確認は残る。実行表のバージョン表示には更新直後も旧表示が残ったため、デプロイ参照の確認にはCLIを使用した。

### v10の同条件追加計測と再開待ち

同じChrome・公開URL・速度優先・配信autoで、ページを読み直してホーム画像を3回計測。ページ内の画像キャッシュは毎回初期化されるが、GASの一覧キャッシュやGoogle側のキャッシュは消去していない。回数を先に3回と定め、全観測を残した。

| 回 | スクリプト開始から初回表示 | 設定取得 | シーン読込 |
| --- | ---: | ---: | ---: |
| 1 | 7.24秒 | 2.19秒 | 5.04秒 |
| 2 | 8.28秒 | 3.14秒 | 5.14秒 |
| 3 | 6.89秒 | 2.28秒 | 4.60秒 |

3回すべて表示成功、ホーム画像のData URIは1,857,059文字。起動計測の中央値は7.24秒。GASの最初の応答以前は含まない。短い間隔の観測なので、コールドスタートや他の端末・回線の安定性は証明しない。過去の遅い観測も評価記録に保持する。ログは `output/quality-audit/performance-v10-repeat.json` に保存した。

2026-09-08 21:04 JST時点で、音声編集画面は引き続きファイル未選択。ファイル選択待機のタイムアウト、CDP機能の不在、Windows操作接続の不在により、Codexから実ファイル添付の検証を完走できない。追加OAuth認可では解消しない操作ツール側の制約。独立して行える修正・認証確認・追加計測は完了したため、手動選択後に次を再開する。

1. 開いている音声編集画面で `output/quality-audit/quality-audit-tone.wav` を選択し、波形・2秒の入力を確認する（ユーザー操作待ち）。
2. MP3生成、情報スポットへの添付保存、閲覧側での再生・停止、infoとDriveの参照を実確認する。
3. 新規写真の添付保存・表示も確認し、検証用マーカーと管理対象の添付をアプリから削除して、シートとDriveに残らないことを確認する。
4. 保存・削除を含む実操作の所要時間と結果を加え、4部門・総合を最終採点する。未確認項目を加点して90点に合わせない。

入力途中の「品質確認・音声添付」は未保存であり、この入力準備ではDriveファイルやinfo行を新規作成していない。

### 音声の実添付検証と第7回改善（バージョン11）

- ユーザーが選択した約2分28秒・2.8MBのMP3を実音声編集画面で読み込み、開始0秒・終了2秒を時刻入力で指定。2秒・48KB・48kHz・192kbps・stereoのMP3生成と「この音声を添付」を確認した。
- 「保存する」で音声付き情報スポットを保存し、infoの14列目に音声ID、13列目にマーカーIDが入ることを読み戻した。Driveではアプリの添付用audioフォルダに48KBのMP3が1件作成された。
- 編集キーのない公開URLからマーカーを開き、音声取得完了後も自動再生しないことを確認。再生ボタンで開始し、プレーヤーが0:02/0:02へ進んで終了した。再生中に情報を閉じ、開き直すと0:00/0:02の停止状態へ戻った。実スピーカーの音質評価はしていない。
- アプリの削除確認から検証マーカーを削除。infoのA1:N4が見出しのみとなり、audioフォルダが空、該当MP3がDriveのゴミ箱にあることを確認。完全削除は行っていない。再読み込みした公開画面にも検証マーカーは残っていない。
- 証拠はGit対象外の `info-audio-saved.tsv`、`info-audio-deleted.tsv`、`drive-audio-saved.txt`、`drive-audio-deleted.txt`、`drive-audio-trashed.txt`、`executions-audio.txt` に保存。
- GAS実行履歴では、初回音声保存53.373秒、公開音声取得12.435秒・7.486秒、添付付き削除23.652秒。初回保存では添付用のルート・photos・audioフォルダも作成された。保存・削除の待ち時間は改善余地があり、完走だけで速度部門を合格にはしない。
- 調査で通常の保存・更新・削除にも毎回14列の列幅とヘッダー書式を再設定していることを確認。v11では現行スキーマが正常な通常操作でこの書式再設定を省いた。既存の欠損ID補修、旧スキーマ移行、未知スキーマでの中止は維持し、明示的な初期設定・更新では標準書式を適用する。
- 書式再設定の回帰テストが修正前に失敗し、修正後に成功。Node全体393件成功、失敗0件。GASバージョン11へ反映し、別フォルダへの読み戻しで5ファイルすべて一致。
- v11実画面で添付なしの検証マーカーを保存・削除し、保存時のinfo行と削除後の空欄を読み戻した。対応するGAS実行は保存2.259秒・削除3.318秒。前述の音声付き操作とは条件が異なるため、53秒・24秒からの同条件改善としては扱わない。添付付きの再計測は写真検証で続ける。今回作成した検証行は残していない。

### 写真の実添付検証と第8回改善（バージョン12）

- ユーザーが選択したJPEGを実画面で処理し、元2.1MB・処理後449KBのプレビューを確認。新規添付として保存し、infoの写真IDとDriveのphotosフォルダの449KBファイルを確認した。
- 編集キーのない公開画面で写真表示・拡大・Escによる拡大終了を確認。説明だけを更新し、写真ID・マーカーIDが変わらないことをシートで照合した。
- アプリの削除確認から検証マーカーを削除。infoは見出しだけになり、photosフォルダは空、対象JPEGはDriveのゴミ箱へ移動した。原画像フォルダのシーンは削除していない。
- 添付フォルダの範囲確認で、同じ共通の親を繰り返し取得していた。v12では構造を読む関数の呼び出し内だけで親一覧を共有する。呼び出し元へ渡すコンテキストは変更せず、Drive移動・作成処理や次回リクエストでは必ず再取得する。フォルダ外・異常な親・移行ジャーナルの検証は維持する。
- 回帰テストで共通親へのDrive参照が1回のフォルダ確認につき4回から2回になることを確認。読取・書込前検証の両方を確認し、次回呼び出しで再取得すること、親を画像フォルダ配下へ移すと拒否することも検証した。テスト失敗を確認後に修正し、Node全体394件成功、失敗0件。
- GASバージョン12へ反映、別フォルダへの読み戻しは5ファイル一致。最終ブラウザ全体は315件成功・10件スキップ・失敗0件（4.2分）。スキップはbefore/afterの画像取得専用モードに依存するケースで、成功件数には含めない。CIではなくローカル実行結果。
- GAS実行履歴の確定値は、v11の新規写真保存17.243秒・公開写真取得7.143秒、v12の公開写真取得7.180秒・写真を維持した更新5.726秒・写真付き削除9.481秒。写真取得の実時間短縮はこの少数回では確認できず、Drive参照回数の削減と区別する。履歴のバージョン欄は現在のデプロイ表示へ変わるため、実行時刻とデプロイ更新の前後で分類した。
- 削除後のv12公開起動は成功し、スクリプト開始から11.301秒、設定取得4.458秒、シーン読込6.832秒。検証マーカーは表示されない。`performance-final-v12.json` と `executions-photo-v12.txt` にログを保存した。
- 最後にscenesを読み戻して、16行・ID16個・ホーム1個・順序1～16・空の表示名0件・画像フォルダ外0件を確認。監査前のID・表示名・親・種別・ホーム・順序の6項目は全件一致。結果は `data-final-summary.json`。infoには検証行を残していない。
- 写真の保存・更新・削除の証拠は `info-photo-saved.tsv`、`info-photo-updated.tsv`、`info-photo-deleted.tsv`、`drive-photo-saved.txt`、`drive-photo-deleted.txt`、`drive-photo-trashed.txt` に保存。個別ID・ユーザーのファイル・元データはGitへ追加しない。

### 完了条件との照合

| 要求 | 現時点の証拠と残り |
| --- | --- |
| Codexから編集・GAS反映 | clasp送信と5ファイル読み戻しが成功。環境整備は完了 |
| ブランチでコミット・プッシュ | `improve/quality-audit-20260908`を使用。各改善を順次反映 |
| 実アプリの操作性・ボタン確認 | 閲覧、シーン移動、編集保存・更新・削除、共有、狭い画面、キーボード、実音声・写真添付の保存・表示・削除を確認 |
| レスポンス改善 | 転送量削減、再訪キャッシュ、重複読み取り削減、不要な書式更新の削減を反映し、実測を記録。ばらつきと添付処理の待ち時間は減点に反映 |
| スプシの無駄・整合性 | scenes16件の主要6項目が監査前と一致。infoの検証行なし。音声・写真の管理対象添付はゴミ箱へ移動済み |
| 全部門・総合90点以上 | 本記録の評価基準で操作性93、速度90、ボタン・機能94、データ品質96、総合93.25。適用範囲と減点理由は冒頭に明記 |

### 観測範囲と運用上の制約

- 実操作は今回指定されたGAS・Drive・シートとChrome環境での確認。異なる回線・多数同時接続・長時間停止後のコールドスタートの性能は保証しない。
- スマートフォンの実センサー許可とQRの実カメラ読取は未実施。狭い画面・権限待機・キーボード・テーマの回帰はローカルブラウザで検証した。
- アップロードはユーザーがOSのファイル選択を担当し、それ以降の生成・保存・閲覧・削除はCodexが実操作した。
- ゴミ箱へ移動した検証添付は復元可能。空の初期シートや通常の空き行・予備列は、不要な業務データと見なして削除していない。
